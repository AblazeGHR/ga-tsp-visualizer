# ga-tsp-visualizer —— 理解文档

> 目的：讲清这个项目在做什么、算法逻辑，以及**它与刘晟材老师工作的关系**（诚实标注：直接相关 / 领域基础，均附可验证来源）。

---

## 1. 项目是什么

用**遗传算法（GA）解旅行商问题（TSP）**的演示项目，并对比局部搜索与精确解：
- GA（锦标赛选择 / 顺序交叉 OX / 交换变异 / 精英保留）找全局近似最优；
- **2-opt 局部搜索**做精化；
- 基准：n≤10 用穷举精确解，n=20 用 OR-Tools 求解器；
- 输出：收敛曲线、最优路径图、2-opt 改进前后对比图 + 结果表。

文件：`tsp_ga.py`（算法）· `main.py`（入口+作图）· `README.md` · `requirements.txt` · `results/`（5 张图）。

运行：
```bash
E:/software/miniforge/python.exe -m pip install -r requirements.txt
E:/software/miniforge/python.exe -m pip install ortools
cd "D:/notes/Ablaze/pages/理工/计算机/申请导师快速练习项目/ga-tsp-visualizer"
E:/software/miniforge/python.exe main.py
```

## 2. 结果（seed=42，可复现）

| 实例 | 纯 GA gap | GA+2-opt gap |
|---|---|---|
| n=10（基准 2.6414，穷举） | 0.00% | 0.00% |
| n=20（基准 3.5232，OR-Tools） | 26.54% | **0.00%** |

**读法**：GA 负责全局探索，2-opt 把局部差距抹平到 0——"全局搜索 + 局部精化"混合思想。

## 3. 与刘晟材老师工作的关系（务必看这个）

### 🔴 直接相关（刘本人的工作/研究方向）
1. **组合优化是他的核心应用域**。他的论文大量处理 TSP / VRP / 背包等 NP-hard 问题（如 *Memetic Search for Vehicle Routing with Simultaneous Pickup-Delivery and Time Windows*，Swarm Evol. Comput. 2021；*Memetic Search for Green VRP*，IEEE TEVC 2025）。本项目用到的 GA+2-opt（模因搜索思想）正是他擅长的**模因搜索（Memetic Search）= 全局进化 + 局部搜索**的简化演示。
   - 来源：他的主页论文列表 https://senshinel.github.io
2. **直接对应他的一篇评估论文**：*How Good Is Neural Combinatorial Optimization? A Systematic Evaluation on the Traveling Salesman Problem*（IEEE CIM 2023，arXiv:2209.10913）。那篇系统比较"神经方法 vs 传统方法"，本项目做的"GA vs 2-opt vs 精确解"就是同一评估视角的手工算法版——展示你理解"基准对比、opt gap"这套评估方法论。
3. **站在他研究主线的"起点"**：刘的主线是 **L2O（Learn to Optimize）——用学习自动设计算法**（如用 LLM 当进化算子，CEC 2024）。本项目是"手工设计算法"的一端；理解这端，才懂他为什么用学习替代它。**这是给导师讲故事的入口**："我理解了经典方法的上限，所以想学怎么用学习突破它。"

### 🟡 领域基础（不是刘的工作，但理解他的工作必需）
- **演化算法**：刘在中科大的博士导师是姚新、唐珂（演化计算领域代表人物），演化算法是他很多工作的底层工具。本项目是你理解"进化机制"的最小实现。
- **TSP**：组合优化的"果蝇"，NCO 文献的标准测试床，几乎所有相关论文都用它做实验。

### 明确不是
- 本项目**不是** L2O / 神经方法——它是传统启发式。它与导师的连接是"评估视角 + 模因搜索思想 + L2O 的对照基线"。

## 4. 学习要点（看代码学什么）
- GA 五要素在代码里的实现位置（选择/交叉/变异/精英保留）；
- opt gap 的意义与计算；
- 2-opt 为何有效（删两条边交叉重连）；
- 收敛曲线怎么读（x 轴代数、y 轴最优成本，平台期=收敛）。

## 5. 升级方向（对应下一批项目）
- 把这个项目的 GA 换成**神经组合优化模型**（见 `nco-tsp` 项目）；
- 把人工算子换成 **LLM 当进化算子**（复现 LMEA，CEC 2024 刘的一作论文）；
- 用 EvoX 框架重写以支持更大规模。
